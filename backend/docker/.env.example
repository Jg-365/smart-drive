# Copie para .env nesta pasta antes de rodar `docker compose up -d`.
# Valores devem corresponder ao DATABASE_URL em backend/smart-drive/.env.
POSTGRES_USER=smartdrive
POSTGRES_PASSWORD=smartdrive
POSTGRES_DB=smartdrive
POSTGRES_PORT=5432
