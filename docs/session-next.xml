<?xml version="1.0" encoding="UTF-8"?>
<!--
  INSTRUÇÕES PARA O CLAUDE CODE:
  Este arquivo contém o PLANEJAMENTO do que vem a seguir.
  - Leia antes de iniciar uma nova sessão para entender prioridades e gargalos.
  - Atualize ao identificar novos bloqueios, dependências ou mudanças de prioridade.
  - Nunca sobrescreva — mova itens concluídos para <completed_from_plan> e adicione novos.
  - session-next.xml é o mapa do futuro. session-current.xml é o estado do presente.
-->

<session_plan id="plan-002" target_session="session-003+" supersedes="plan-001">

  <goal>
    Plano de implementação em épicos para TODOS os requisitos do João (JOA-RF-01..08,
    JOA-RNF-01..05, JOA-TEC-01..04), elaborado em 2026-06-09 considerando o estado real do
    código (frontend protótipo avançado, backend/firmware em esqueleto). Plano completo
    salvo em /home/joao/.claude/plans/wobbly-coalescing-comet.md e aprovado por João.
  </goal>

  <!-- ═══════════════════════════════════════════════════════════
       ÉPICOS — ORDEM RECOMENDADA
       Cada épico = uma sessão de trabalho. Ao concluir, mover para
       <completed_from_plan> e atualizar requirements_status em
       session-current.xml.
       ═══════════════════════════════════════════════════════════ -->
  <epics>
    <!-- TODOS os épicos do plan-002 (EPIC-00..13) foram implementados. Nenhum épico do João pendente.
         JOA-TEC-02 FECHADO na bancada (2026-06-17, ESP-IDF 6.0, PR #13) → B02 resolvido. Tela MAPA do
         desktop e indicadores reais do shell entregues (PR #14, JOA-RF-04). O "100% pronto" do
         JOA-TEC-04 ainda depende de itens de Pedro/Nathan. Sprint de correção da auditoria-001
         (2026-06-17, FIX-01..07, PRs #16..#22) já corrigiu a parte do João; setGlobalPrefix('api')
         aplicado. Tudo que é de Pedro/Nathan está agora com CONTRATO + pré-pronto em
         docs/bloqueios-equipe-001.xml.
         Próximas frentes do João são de INTEGRAÇÃO (não novos épicos):
           - quando Pedro entregar auth/login + endpoints reais (devices/trips/ingestão): preencher o
             auth store (token), apontar lib/api do MSW p/ o backend, remover o controller mock de
             telemetria (ver docs/bloqueios-equipe-001.xml);
           - quando Nathan emitir score/consumo/eventos via WS: validar no dashboard/relatório/demo;
           - scrub de textos longos na MobileDemoPage (CHK-08, secundário);
           - auditoria RNF-03 (44px) e RNF-05 (rodar do zero noutra máquina);
           - dívidas do firmware: migrar driver I2C legado p/ driver/i2c_master.h (EOL no ESP-IDF 7.0)
             e resolver o timestamp tempo-desde-boot (SNTP ou carimbo no backend — alinhar contrato c/ Pedro).
         INTEGRAÇÃO DOS ANALYTICS DO NATHAN — CONCLUÍDA (2026-06-17, branches feature/joa-integ-00..04):
         score (trip:scoreUpdated) e eventos (trip:eventDetected) reais fluindo backend→WS→store→
         dashboard/relatório/demo; simulador dirigido ao vivo pelo DemoModule (/api/demo/*); provider do
         front assina o tripId ativo (dívida #6 resolvida). Falta: abrir/mergear os PRs INT-00..04 na ordem.
         AINDA PENDENTE (não é do João): CONSUMO (trip:fuelEstimateUpdated) — WIP do Nathan
         (FuelEstimationService vazio); quando ele entregar, o orquestrador emite e a UI sai de "—".
         Pesos de penalidade do score e realismo dos cenários seguem do Nathan. -->
  </epics>

  <!-- ═══════════════════════════════════════════════════════════
       GARGALOS CONHECIDOS (herdados de plan-001 + novos)
       ═══════════════════════════════════════════════════════════ -->
  <bottlenecks>
    <bottleneck id="G01" impact="critical" status="resolved" resolved_date="2026-06-09">
      Schema Prisma (EPIC-00) implementado e migrado contra Postgres local. Pedro e Nathan já
      podem implementar seus módulos sobre prisma/schema.prisma e PrismaService.
    </bottleneck>
    <bottleneck id="G02" impact="high" status="resolved" resolved_date="2026-06-15">
      RESOLVIDO no EPIC-03: instaladas @tanstack/react-query, zustand, socket.io-client e
      maplibre-gl; lib/api/ e features/shared/realtime/ (store + useTelemetrySocket +
      TelemetryProvider) prontos e testados. EPIC-05/06/08/09 já podem consumir o provider/store.
    </bottleneck>
    <bottleneck id="G03" impact="medium" status="resolved" resolved_date="2026-06-15">
      RESOLVIDO no EPIC-06: MapLibre carregado via next/dynamic ssr:false (LiveMapContainer em
      MapPage); import do maplibre-gl fica fora do server bundle, build do Next passa. Original:
    </bottleneck>
    <bottleneck id="G03-old" impact="medium" status="resolved">
      MapLibre pode ter conflitos com SSR do Next.js 16/React 19 — usar dynamic import com
      ssr:false e testar cedo no EPIC-06.
    </bottleneck>
    <bottleneck id="G04" impact="medium" status="open">
      GPS indoor instável — simulador do Nathan + pista virtual cobrem isso (EPIC-09).
    </bottleneck>
  </bottlenecks>

  <!-- ═══════════════════════════════════════════════════════════
       DEPENDÊNCIAS ENTRE TIMES (herdadas)
       ═══════════════════════════════════════════════════════════ -->
  <dependencies>
    <dep from="Nathan" to="João" description="Nathan precisa do schema Prisma (EPIC-00) e do endpoint /telemetry (Pedro/EPIC-04 mock) para implementar score e consumo" />
    <dep from="Pedro" to="João" description="Pedro precisa do schema Prisma (EPIC-00) para implementar auth e cadastros" />
    <dep from="João" to="Pedro" description="João precisa dos endpoints de auth e viagem do Pedro para EPIC-02/07/08/09 — usar mocks/contratos enquanto não prontos" />
    <dep from="João" to="Nathan" description="João precisa do score e consumo do Nathan para EPIC-05/08 — usar mocks MSW com mesma interface até integração" />
  </dependencies>

  <!-- ═══════════════════════════════════════════════════════════
       PERGUNTAS EM ABERTO (herdadas + status)
       ═══════════════════════════════════════════════════════════ -->
  <open_questions>
    <question id="Q01" priority="high" status="resolved">
      Tipos TypeScript compartilhados: RESOLVIDO na prática — frontend já definiu tipos maduros
      em features/shared/types/. Decisão para EPIC-00: usar esses tipos como fonte de verdade
      do schema.prisma (Opção A informal).
    </question>
    <question id="Q02" priority="medium" status="open">
      Pedro vai implementar JWT na semana 1 ou 2? EPIC-02 já saiu com TempUserGuard
      (header x-user-id, contrato request.user.id) — EPIC-07 e demais módulos (Devices/
      Trips/Telemetry) devem trocar para o guard real do Pedro assim que disponível, mantendo
      o mesmo contrato.
    </question>
    <question id="Q03" priority="medium" status="resolved">
      RESOLVIDO na integração (2026-06-17): score é um serviço NestJS (@Injectable DrivingScoreService),
      mas como tem estado por viagem, o AnalysisOrchestratorService o instancia POR tripId via `new`
      (factory), não como provider singleton. Detectores são classes simples instanciadas por viagem.
      Consumo (FuelEstimationService) ainda é WIP do Nathan.
    </question>
    <question id="Q04" priority="low" status="open">
      Tile server do MapLibre: tiles públicos (OpenFreeMap) vs locais para funcionar offline
      na ExpoIOT — decidir no EPIC-06.
    </question>
  </open_questions>

  <!-- ═══════════════════════════════════════════════════════════
       ITENS CONCLUÍDOS DO PLANO ANTERIOR (plan-001)
       ═══════════════════════════════════════════════════════════ -->
  <completed_from_plan>
    <item from="plan-002" task="EPIC-13">
      QA final &amp; apresentação ExpoIOT (JOA-TEC-04). QA-CHECKLIST.md (7 SPECs + RNFs + dependências
      mapeados a status, + roteiro de smoke) e APRESENTACAO-EXPOIOT.md (roteiro de palco). Smoke
      automatizado verde: frontend 220 testes + tsc + build; backend 57 testes serial (DTO + integração
      Postgres + gateway WS); docker + migrate OK. Branch test/joa-tec-04-qa-final, PR #12. Concluído
      (entregáveis) em 2026-06-16. JOA-TEC-04 segue in_progress até o e2e + integrações dos times +
      build do firmware — tudo em QA-CHECKLIST.md.
    </item>
    <item from="plan-002" task="EPIC-12">
      Não-funcionais + botões mortos (JOA-RNF-02/03/04/05). Btn com prop disabled; navegação ligada
      (INICIAR VIAGEM/MODO DEMO → demo; VOLTAR → live) via callbacks do app/page.tsx; cosméticos e
      devices sem feature desabilitados (nenhum botão morto). scripts/dev-up.sh + dev-down.sh (RNF-02).
      RNF-04 done (seletores atômicos + downsample). 4 testes novos, suite 216 → 220. Branch
      feature/joa-rnf-02-05-polish, PR #11. Concluído em 2026-06-16. PENDENTE p/ EPIC-13: auditoria
      mobile 375px/44px (RNF-03) e checklist de portabilidade (RNF-05).
    </item>
    <item from="plan-002" task="EPIC-11">
      Firmware ESP32/FreeRTOS (JOA-TEC-02) — IMPLEMENTAÇÃO COMPLETA, build a validar. Pipeline modular
      em firmware/main/: imu_reader (I2C MPU6050), gps_reader (NMEA), sensor_fusion (média móvel),
      event_detector (limiares; event_detector_eval puro), net_client (Wi-Fi + POST contrato v1.0 via
      cJSON), health_monitor; estado por mutex + filas; sd_config.h centraliza tudo; README do firmware.
      Branch firmware/joa-tec-02-esp32, PR #10. Concluído (código) em 2026-06-16. BLOQUEIO B02: sem
      toolchain ESP-IDF no ambiente do Claude → João precisa rodar `idf.py build` (ESP-IDF >= 5.0) +
      validar com hardware (MPU6050 + NEO-6M) para fechar o done_when. JOA-TEC-02 segue in_progress
      até o build passar.
    </item>
    <item from="plan-002" task="EPIC-10">
      Tema &amp; design system: polimento final (JOA-RF-06/07). useTheme com localStorage seguro
      (modo privado) + DARK default real (sem prefers-color-scheme) + lazy init (corrige lint) +
      script anti-flicker no layout. Auditoria WCAG AA (contrast.ts + testes, dark e light). MapLibre
      tema-aware (basemap.ts). Logos oficiais: favicon=preto sem fundo (Vector), apple-icon=thumb (PWA),
      BrandLogo no header (branca no dark / completo no light); logos-fonte versionados em /assets.
      19 testes novos, suite 197 → 216. Branch feature/joa-rf-06-07-theme-polish, PR #9. Concluído em
      2026-06-16. NOTA: 3 erros de ESLint pré-existentes em map-gauges.tsx (refatoração à parte).
    </item>
    <item from="plan-002" task="EPIC-09">
      Modo Demo ExpoIOT (JOA-RF-05) — frontend. lib/api/demo.ts (startDemo/resetDemo/fetchCurrentDemo),
      features/demo/hooks.ts (React Query), DemoPage/MobileDemoPage como painel de controle real
      (iniciar 1 clique, reset idempotente, trava contra sessão dupla, veículo padrão/seleção, perfil,
      stage ao vivo via store, pista virtual p/ GPS ausente). 7 testes novos, suite 190 → 197. Branch
      feature/joa-rf-05-demo-mode, PR #8. Concluído em 2026-06-16. PENDÊNCIAS: (1) provider não
      re-assina o tripId da demo (telemetria demo ao vivo não flui até elevar o tripId ao
      TelemetryProvider); (2) backend DemoModule real e integração com simulador do Nathan (NAT-RF-07)
      / carrinho RC ficam para QA/integração (EPIC-13).
    </item>
    <item from="plan-002" task="EPIC-08">
      Relatório de viagem (JOA-RF-08). lib/api/trips.ts (fetchTrips/fetchTripSummary/fetchTripRoute),
      features/trips/derive.ts (formatDuration hh:mm:ss, classifyScore, formatDistance sem-GPS,
      buildRecommendations), hooks React Query, TripReportPage/MobileTripReportPage reescritas com
      dados reais (distância/duração/vel/score/consumo/eventos/trajeto/recomendações + loading/erro/
      vazio). 10 SPECs + 5 edges (0km, 0 eventos, sem GPS, score 0/100). 22 testes novos, suite
      168 → 190. Branch feature/joa-rf-08-trip-report, PR #7 (a revisar/mergear). Concluído em
      2026-06-16. PENDÊNCIA: classificação derivada de trip.drivingScore (o /summary ainda não traz
      o DrivingScore detalhado do Nathan — NAT-RF-05); integrar quando o payload incluir penalties.
    </item>
    <item from="plan-002" task="EPIC-07">
      Cadastro/edição de veículos no frontend (JOA-RF-01/02). lib/api/vehicles.ts (CRUD tipado),
      schema Zod espelhando o CreateVehicleDto, hooks React Query com invalidação (UI atualiza na
      hora), QueryProvider (features/shared/query) plugado em app/page.tsx, VehicleForm (erros por
      campo + erro de servidor) e VehiclesPage reescrita (lista/detalhe/criar/editar/excluir reais,
      estados loading/erro/vazio, 404 cross-user amigável). zod@4 instalado. 25 testes novos, suite
      143 → 168. Corrigidos de brinde os 6 erros tsc de factories.test.ts. Branch
      feature/joa-rf-01-02-vehicles-frontend. Concluído em 2026-06-16. PENDÊNCIAS de integração:
      (1) prefixo /api vs /vehicles no backend; (2) cliente HTTP ainda não envia header de auth
      (x-user-id/JWT) — TempUserGuard do backend exigirá isso; (3) schema Zod duplica o DTO.
    </item>
    <item from="plan-002" task="EPIC-06">
      Mapa em tempo real com MapLibre GL JS (JOA-RF-04). features/shared/geo.ts (isValidCoord,
      toLngLat, downsample 500). Store.route acumulada (só coord válida). LiveMap (MapLibre
      imperativo: style raster escuro CARTO inline, NavigationControl/zoom, polyline com
      downsampling, marcadores de evento + veículo, centralização no 1º ponto, fallback de tiles).
      LiveMapContainer liga o store; MapPage via next/dynamic ssr:false (resolve G03). 15 testes
      novos, suite frontend 143/143. Branch feature/joa-rf-04-maplibre-map. Concluído em 2026-06-15.
      Q04 decidido: style raster escuro CARTO inline (self-contained, dark, sem vector tile server),
      override via styleUrl. NOTA: dashboard/mini-mapas seguem em SVG; GET /trips/:id/route (Pedro)
      pendente.
    </item>
    <item from="plan-002" task="EPIC-05">
      Dashboard em tempo real (JOA-RF-03). DashboardPage/MobileLivePage ligados ao store via
      seletores atômicos; 12 SPECs cobertos (velocidade+guard inválido, accel, GPS/placeholder,
      online/offline por staleness via useLiveStatus, eventos, score, consumo, sem refresh, banner
      de reconexão, estado vazio+CTA). Helpers em features/dashboard/derive.tsx; store ganhou
      lastPacketAt. TelemetryProvider plugado em app/page.tsx (tripId via NEXT_PUBLIC_DEV_TRIP_ID).
      Dev feeder em tools/telemetry-feeder/ (POST /telemetry em loop). Favicon app/icon.svg.
      26 testes novos, suite frontend 132/132. Branch feature/joa-rf-03-dashboard-realtime.
      Concluído em 2026-06-15. LIMITAÇÃO: score/consumo/eventos dependem do caminho do Nathan
      (trip:scoreUpdated/fuelEstimateUpdated/eventDetected); o feeder só dispara telemetry:new.
    </item>
    <item from="plan-002" task="EPIC-03">
      Camada de dados real-time do frontend (JOA-RNF-01). Instaladas @tanstack/react-query,
      zustand, socket.io-client, maplibre-gl. lib/api/ (client fetch tipado + ApiError, config,
      fetchLiveTelemetry/fetchTripTelemetry). features/shared/realtime/ (store Zustand com selectors
      atômicos, useTelemetrySocket com reconexão 5x + fallback de polling 2s, TelemetryProvider).
      13 testes novos (socket.io-client e lib/api mockados), suite 106/106. Corrigido bug em
      setupTests.ts (MSW global desligado por require sem resolução de alias). Branch
      feature/joa-rnf-01-realtime-data-layer. Concluído em 2026-06-15 — detalhes em
      session-current.xml. Hook pronto para plugar nas páginas (EPIC-05/06).
    </item>
    <item from="plan-002" task="EPIC-04">
      Backend WebSocket Gateway (JOA-RF-03 infra). TelemetryGateway (socket.io via
      @nestjs/platform-socket.io) com rooms por viagem (subscribe:trip/unsubscribe:trip), emite
      telemetry:new e device:statusChanged só para quem assinou. TelemetryPayloadDto valida o
      contrato v1.0; telemetry.mapper achata gps/imu no LiveTelemetryPoint (tripId=sessionId).
      TelemetryController mock/dev TEMPORÁRIO (POST /telemetry dispara o gateway) até o endpoint
      real do Pedro (PED-RF-06). 16 testes (DTO, gateway unit, integração com socket real provando
      o done_when), suite 57/57 serial. Adicionado setupFiles reflect-metadata ao jest (corrige
      flake de "Reflect.getMetadata is not a function" nos specs de DTO). Branch
      feature/joa-rf-03-websocket-gateway (a partir de origin/main). Concluído em 2026-06-15 —
      detalhes em session-current.xml. Destrava o EPIC-05.
    </item>
    <item from="plan-002" task="EPIC-02">
      VehiclesModule completo (POST/GET/PATCH/DELETE /vehicles) em
      backend/smart-drive/src/vehicles/, com TempUserGuard placeholder em src/auth/
      (request.user.id, a substituir pelo guard JWT do Pedro). 35 testes (DTO + integração
      Postgres real) passando, smoke test via curl ok. Branch
      feature/joa-rf-01-02-vehicles-module (primeira branch isolada por épico). Concluído em
      2026-06-09 — ver detalhes em session-current.xml.
    </item>
    <item from="plan-002" task="EPIC-01">
      docker-compose.yml + .env.example (backend, docker, frontend) + README.md raiz com setup
      passo a passo (banco → backend → frontend), validado de ponta a ponta. Concluído em
      2026-06-09.
    </item>
    <item from="plan-002" task="EPIC-00">
      Schema Prisma completo (8 models, 6 enums) + migration `20260609234700_init` aplicada;
      PrismaService/PrismaModule globais com @prisma/adapter-pg; ConfigModule + ValidationPipe
      globais; módulos placeholder (Vehicles/Devices/Trips/Telemetry/Demo/Analysis); 6 testes
      de integração passando contra Postgres real via docker compose. Concluído em 2026-06-09 —
      ver detalhes em session-current.xml.
    </item>
    <item from="plan-001" task="T09/T10 (parcial)">
      Setup Next.js + Tailwind + estrutura de pastas + design system (sd-tokens/sd-vars) +
      ThemeProvider dark/light com testes — feito no sprint-1 (commits feat/s1-*),
      embora T09 original previsse shadcn/ui + libs de dados que ainda não foram instaladas.
    </item>
    <item from="plan-001" task="domain types">
      Tipos TypeScript de domínio (features/shared/types/*) com enums — feito no sprint-1,
      mais completo que o T03 original previa para esta fase.
    </item>
    <item from="plan-001" task="MSW mocks">
      MSW server + handlers + factories para todos os domínios — feito no sprint-1
      (substitui parte do T08 / dados do simulador para desenvolvimento frontend).
    </item>
  </completed_from_plan>

</session_plan>
