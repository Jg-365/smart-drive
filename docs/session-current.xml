<?xml version="1.0" encoding="UTF-8"?>
<!--
  INSTRUÇÕES PARA O CLAUDE CODE:
  Este arquivo representa o estado ATUAL da sessão de trabalho.
  - Leia este arquivo no início de cada sessão para saber exatamente onde parou.
  - Atualize este arquivo ao completar cada requisito, bloco de trabalho ou decisão importante.
  - Nunca perca informação — acumule o histórico em <history>.
  - Seja específico: "criou VehicleForm component com validação Zod" é melhor que "trabalhou no frontend".
-->

<session id="session-002" date="2026-06-09" status="in_progress">

  <objective>
    Varredura geral do projeto (estado real vs. documentado), correção da documentação de sessão
    (estava desatualizada em relação ao trabalho do sprint-1 já commitado) e elaboração do plano
    de implementação em épicos para os requisitos do João.
  </objective>

  <current_focus>
    EPIC-00 a EPIC-06 concluídos. EPIC-02/03/04/05 em main (PRs #1/#2/#3/#4 mergeados). EPIC-06
    (Mapa MapLibre real, JOA-RF-04) implementado na branch feature/joa-rf-04-maplibre-map: MapPage
    com MapLibre GL JS (LiveMap + LiveMapContainer, via next/dynamic ssr:false), geo.ts (downsample/
    validação), store.route acumulada, fallback de tiles. 143/143 testes frontend, build OK. PR a
    abrir. Gargalo G03 (SSR do MapLibre) resolvido com dynamic import ssr:false.
    Próximo: EPIC-07 (cadastro/edição de veículos no frontend, JOA-RF-01/02) ou EPIC-08/09/10/11.
    Ver session-next.xml.
  </current_focus>

  <!-- ═══════════════════════════════════════════════════════════
       ESTADO POR REQUISITO
       Status: not_started | in_progress | blocked | done | skipped
       ═══════════════════════════════════════════════════════════ -->
  <requirements_status>

    <req id="JOA-TEC-01" title="Banco de dados" status="done">
      <notes>
        backend/smart-drive/prisma/schema.prisma completo: 8 models (User, Vehicle, Device,
        Trip, TelemetryPoint, DrivingEvent, FuelEstimate, RefuelRecord) + 6 enums (FuelType,
        DeviceStatus, TripMode, TripStatus, DrivingEventType, EventSeverity), derivados de
        features/shared/types/* com notas de design no topo do arquivo para Pedro/Nathan.
        Migration `20260609234700_init` aplicada com sucesso contra Postgres 16 local
        (docker compose). PrismaService/PrismaModule (global, com @prisma/adapter-pg) e
        ConfigModule/ValidationPipe globais configurados. Módulos placeholder criados:
        VehiclesModule, DevicesModule, TripsModule, TelemetryModule, DemoModule,
        AnalysisModule. `nest build` e `npm test` (6 testes de integração Prisma) passam.
      </notes>
    </req>

    <req id="JOA-TEC-02" title="Firmware e hardware" status="in_progress">
      <notes>
        Projeto ESP-IDF inicializado: firmware/main/main.c e CMakeLists.txt existem,
        firmware/build/ presente (já compilado ao menos uma vez). Nenhuma FreeRTOS task,
        driver de IMU/GPS ou lógica de sensor_fusion implementada ainda.
      </notes>
    </req>

    <req id="JOA-TEC-03" title="DevOps local" status="done">
      <notes>
        backend/docker/docker-compose.yml (postgres:16-alpine, volume nomeado
        smartdrive_postgres_data, healthcheck) criado e testado — `docker compose up -d`
        sobe o banco. .env.example criados em backend/docker, backend/smart-drive e
        frontend/smart-drive (com `!.env.example` no .gitignore do frontend, que tem
        `.env*` ignorado). README.md raiz reescrito com passo a passo completo
        (banco → backend → frontend). Validado: `npx prisma migrate dev` idempotente
        e `npm run start:dev` sobe limpo conectando ao Postgres real.
      </notes>
    </req>

    <req id="JOA-TEC-04" title="QA e testes finais" status="not_started">
      <notes>Depende de TEC-01 a TEC-03 e dos módulos de Pedro/Nathan. Nada a testar ainda em integração.</notes>
    </req>

    <req id="JOA-RF-01" title="Cadastro de veículo" status="done">
      <notes>
        Backend: VehiclesModule completo (POST/GET/PATCH/DELETE /vehicles) em
        backend/smart-drive/src/vehicles/. CreateVehicleDto com class-validator cobre todos os
        SPECs/edge cases (consumos e tanque devem ser positivos, marca/modelo obrigatórios e
        tratados como vazios se só espaços, fuelType enum, ano em [1900, anoAtual+1], peso
        opcional). sanitizeText() remove caracteres especiais de marca/modelo sem rejeitar
        (mantém acentos/hífen/apóstrofo/ponto/parênteses). VehiclesService usa PrismaService;
        veículo sempre criado com ownerId do usuário autenticado. 35 testes (unit DTO +
        integração contra Postgres real) + smoke test via curl, todos passando.
        Frontend (VehiclesPage com dados mockados) ainda não conectado — fica para EPIC-07.
      </notes>
    </req>

    <req id="JOA-RF-02" title="Edição de veículo" status="done">
      <notes>
        PATCH /vehicles/:id (UpdateVehicleDto = PartialType de CreateVehicleDto) atualiza só os
        campos enviados, preserva os demais (Prisma ignora chaves undefined). Salvar sem alterar
        nada (`{}`) não gera erro nem efeito colateral. Tentativa de editar/deletar/ler veículo de
        outro usuário retorna 404 (NotFoundException — não revela existência). Soft delete via
        `deletedAt`; findAll/findOne já filtram deletedAt=null. "Trip ativa usa snapshot anterior"
        é suportado pelo schema (Trip.vehicleSnapshot, EPIC-00) — não há lógica adicional aqui,
        pois criação de Trip é do Pedro. Frontend (VehicleForm) fica para EPIC-07.
      </notes>
    </req>

    <req id="JOA-RF-03" title="Dashboard em tempo real" status="done">
      <notes>
        EPIC-05 (branch feature/joa-rf-03-dashboard-realtime): DashboardPage (desktop) e
        MobileLivePage (mobile) ligados ao store via seletores atômicos (RNF-01). Cobertos os 12
        SPECs de JOA-RF-03: velocidade do último ponto (com guard isValidSpeed, edge >300km/h =
        "dado inválido"), aceleração, GPS ou "GPS indisponível" (lat/lng null), status online/
        offline (useLiveStatus: staleness — offline após 5s sem pacote mesmo com socket aberto,
        volta a online sozinho), eventos recentes (feed do store, burst), score parcial +
        classificação, consumo estimado parcial, atualização reativa sem refresh, banner de
        reconexão (reconnecting/polling), estado vazio + CTA quando tripId null. Helpers comuns em
        features/dashboard/derive.tsx (G, SCORE_HINT, EVENT_META, isHighSeverity). Store ganhou
        lastPacketAt (em ingestPoint). TelemetryProvider plugado em app/page.tsx com tripId de dev
        (NEXT_PUBLIC_DEV_TRIP_ID, null em prod → estado vazio). Dev feeder em
        tools/telemetry-feeder/ (POST /telemetry em loop, exercita gateway→WS→store→dashboard).
        26 testes novos (useLiveStatus 7, DashboardPage 14, MobileLivePage 5); suite frontend
        132/132. tsc/eslint limpos. NOTA: score/consumo/eventos só atualizam quando o caminho do
        Nathan (trip:scoreUpdated/fuelEstimateUpdated/eventDetected) emitir — o feeder dispara só
        telemetry:new; até lá ficam vazios/"—".
        Histórico INFRA: frontend TelemetryProvider/useTelemetrySocket (EPIC-03) +
        TelemetryGateway backend (EPIC-04), ambos em main.
        EPIC-04 (backend WebSocket Gateway): TelemetryModule deixou de ser placeholder —
        TelemetryGateway (socket.io via @nestjs/platform-socket.io) com rooms por viagem
        (subscribe:trip/unsubscribe:trip → room trip:&lt;id&gt;), emite telemetry:new e
        device:statusChanged só para quem assinou; TelemetryPayloadDto valida o contrato v1.0
        (deviceId/timestamp obrigatórios, accelX/Y/Z obrigatórios, GPS null aceito, lat/lng em
        range); telemetry.mapper achata o contrato (gps/imu aninhados) no LiveTelemetryPoint que o
        front consome (tripId = sessionId); TelemetryController é mock/dev TEMPORÁRIO (POST
        /telemetry dispara o gateway; POST /telemetry/dev/device-status) até o POST /telemetry real
        do Pedro (PED-RF-06), que deve chamar gateway.emitTelemetryNew após persistir. 16 testes
        (DTO 9, gateway unit 5, integração com socket real 2 — cliente recebe telemetry:new após
        POST, e 400 sem deviceId); suite backend 57/57 (serial). Deps: @nestjs/websockets,
        @nestjs/platform-socket.io, socket.io (+ socket.io-client dev).
      </notes>
    </req>

    <req id="JOA-RF-04" title="Mapa em tempo real" status="done">
      <notes>
        EPIC-06 (branch feature/joa-rf-04-maplibre-map): MapPage agora usa MapLibre GL JS real, não
        mais o MapView SVG. features/shared/geo.ts (puro, testado): isValidCoord, toLngLat ([lng,lat]
        ou null), downsample (uniforme, preserva extremos, MAX_POLYLINE_POINTS=500). Store ganhou
        route: LngLat[] — ingestPoint só acumula coordenada válida (GPS null/fora de range é
        ignorado → polyline não ganha o ponto e o marcador "congela" no último válido). LiveMap.tsx
        (client, MapLibre imperativo): style raster escuro CARTO inline (self-contained, dark),
        NavigationControl (zoom in/out), source/layer de rota (polyline com downsampling), source/
        layer de eventos (círculos por severidade), marcador do veículo, centraliza no primeiro
        ponto, e fallback visual quando o tile server falha (evento map 'error'). LiveMapContainer
        liga o store (route, vehicle=último ponto válido, eventos com coord). MapPage carrega o
        LiveMapContainer via next/dynamic ssr:false (mitiga G03 — MapLibre usa WebGL/window). CSS do
        maplibre importado em app/layout.tsx. 14 SPECs/edges cobertos: marcador na posição atual e
        atualização a cada ponto válido, polyline acumulada, pontos simulados = mesma interface
        (LngLat), marcadores de evento, zoom, viewport mobile/desktop (componente fluido), lat/lng
        null ignorado, downsampling > 500, fallback sem tiles, centralização no 1º ponto. 15 testes
        novos (geo 6, store route 1, LiveMap 4 com maplibre-gl mockado + cobertura via utils). suite
        frontend 143/143, tsc/build limpos. NOTA: integração com GET /trips/:id/route (replay,
        Pedro) fica para depois; aqui a rota vem do fluxo ao vivo (store). O mini-mapa do dashboard
        e telas (MobileLivePage/MapView SVG) seguem em SVG por ora — migração opcional no EPIC-10.
      </notes>
    </req>

    <req id="JOA-RF-05" title="Modo demo ExpoIOT" status="in_progress">
      <notes>
        DemoPage e MobileDemoPage existem como protótipo visual (modos smooth/normal/aggressive
        no app/page.tsx). Sem módulo /demo no backend, sem integração com simulador do Nathan,
        sem lógica real de iniciar/resetar sessão demo.
      </notes>
    </req>

    <req id="JOA-RF-06" title="Tema dark/light" status="done">
      <notes>
        Implementado em features/shared/theme/ (ThemeProvider, useTheme, ThemeToggle) com
        CSS custom properties (lib/sd-vars.ts) e testes (theme.test.tsx). Tema dark é o default.
        Persistência em localStorage a confirmar contra os SPECs de JOA-RF-06.
      </notes>
    </req>

    <req id="JOA-RF-07" title="Estilo visual HQ dark" status="in_progress">
      <notes>
        Design system implementado via lib/sd-tokens.ts / lib/sd-vars.ts (cores grafite,
        ciano/azul elétrico, vermelho de alerta) e componentes primitivos (Stat, Panel, Tag,
        Dot, Btn). Aplicado consistentemente nas telas existentes. Falta auditoria formal de
        contraste WCAG AA e revisão em todos os componentes finais (alguns ainda não migrados
        de dados hardcoded para dados reais, o que pode mudar layout).
      </notes>
    </req>

    <req id="JOA-RF-08" title="Relatório de viagem" status="in_progress">
      <notes>
        TripReportPage e MobileTripReportPage existem (features/trips) como protótipo visual
        com dados mockados. Falta integração com GET /trips/:id/summary, GET /trips/:id/events,
        score/consumo reais do Nathan, e tratamento dos edge cases (0 km, 0 eventos, sem GPS).
      </notes>
    </req>

    <req id="JOA-RNF-01" title="Tempo real percebido" status="done">
      <notes>
        EPIC-03 concluído na branch feature/joa-rnf-01-realtime-data-layer. Instaladas as libs de
        dados (@tanstack/react-query, zustand, socket.io-client, maplibre-gl). Criado lib/api/
        (client fetch tipado api.get/post/patch/delete + ApiError; config com NEXT_PUBLIC_API_URL/
        WS_URL; serviços fetchLiveTelemetry/fetchTripTelemetry para o fallback de polling).
        Criado features/shared/realtime/: store Zustand com selectors atômicos (useConnection,
        useLastPoint, useDrivingScore, etc. — mudar um ponto não re-renderiza cards não
        relacionados), events.ts (nomes dos eventos WS), useTelemetrySocket (conecta, subscribe:trip,
        mapeia os 6 eventos server→client, reconexão 5x com backoff 0.5→8s, fallback de polling 2s
        via lib/api ao esgotar, cleanup completo no unmount) e TelemetryProvider (sobe o hook sem
        assinar o store). SPECs RNF-01 cobertos: reconexão automática, fallback por polling após N
        tentativas, sem re-render desnecessário, base para "mapa não re-renderiza por ponto"
        (consolidação visual fica no EPIC-06). 13 testes novos (hook, store, provider, client) com
        socket.io-client e lib/api mockados; suite frontend 106/106, tsc/eslint limpos nos arquivos
        novos. Bônus: corrigido bug em setupTests.ts (require não resolvia alias @/, MSW global
        estava desligado em toda a suite) trocando por import estático.
      </notes>
    </req>
    <req id="JOA-RNF-02" title="Disponibilidade na demo" status="not_started" />
    <req id="JOA-RNF-03" title="Mobile-first" status="in_progress">
      <notes>Layout mobile (402x780 simulado) e desktop já existem nas telas protótipo, mas não validados contra os SPECs formais (375px, áreas de toque 44x44px).</notes>
    </req>
    <req id="JOA-RNF-04" title="Performance" status="not_started" />
    <req id="JOA-RNF-05" title="Portabilidade" status="not_started" />

  </requirements_status>

  <!-- ═══════════════════════════════════════════════════════════
       DECISÕES TOMADAS NESTA SESSÃO
       ═══════════════════════════════════════════════════════════ -->
  <decisions>
    <decision date="PRE-SESSION" author="João + Claude">
      <what>Stack tecnológica finalizada: NestJS + Next.js + PostgreSQL + Prisma + MapLibre</what>
      <why>Desenvolvimento rápido, TypeScript ponta a ponta, 4 semanas de prazo</why>
      <tradeoffs>Optou-se por NestJS sobre Spring Boot pela velocidade; Next.js sobre Electron pela simplicidade</tradeoffs>
    </decision>
    <decision date="PRE-SESSION" author="João + Claude">
      <what>Monorepo — único repositório com pastas firmware/, backend/, frontend/, simulator/, docs/</what>
      <why>Time pequeno, tipos TypeScript compartilhados, visão unificada do projeto</why>
    </decision>
    <decision date="PRE-SESSION" author="João + Claude">
      <what>IA explicitamente fora do escopo — modelo determinístico de consumo no MVP</what>
      <why>Evitar scope creep, modelo explicável é mais defensável para o avaliador acadêmico</why>
    </decision>
    <decision date="2026-06-09" author="Claude">
      <what>session-current.xml reescrito para refletir o estado real do código (frontend sprint-1 avançado, backend/firmware em estágio de esqueleto)</what>
      <why>O arquivo estava no template "PRE-SESSION / not_started" mesmo após 5 commits de trabalho real do sprint-1 (test infra, MSW mocks, domain types, theme system, feature architecture). Documentação desalinhada do código é um risco para Pedro e Nathan, que dependem destes docs.</why>
    </decision>
    <decision date="2026-06-09" author="João + Claude">
      <what>Prisma 7.8.0 com generator `provider = "prisma-client"`, `output = "../generated/prisma"`, `moduleFormat = "cjs"`, e datasource SEM `url` (Prisma 7 não permite mais `url` no schema). PrismaClient instanciado via @prisma/adapter-pg (driver adapter), lendo DATABASE_URL via ConfigService.</what>
      <why>Prisma 7 mudou a API de configuração: `url` no datasource foi removido (P1012), e o client gerado em ESM (default) quebra o ts-jest do projeto ("Cannot use import.meta outside a module"). `moduleFormat = "cjs"` + driver adapter resolve ambos sem mudar o stack CommonJS/Nest existente.</why>
      <tradeoffs>generated/prisma ainda exige moduleNameMapper no jest (resolver imports `.js"->".ts") e NODE_OPTIONS=--experimental-vm-modules (o query compiler wasm usa dynamic import mesmo em CJS) — já configurado em package.json.</tradeoffs>
    </decision>
    <decision date="2026-06-09" author="João + Claude">
      <what>A partir do EPIC-02, cada épico é desenvolvido em sua própria branch
      (feature/joa-*, infra/joa-*, firmware/joa-*, test/joa-* conforme git_conventions em
      docs/CLAUDE.json), nunca direto em main. Commits sem trailer de coautoria de IA.</what>
      <why>João pediu isolamento por épico para review/merge independente e para não tornar
      main um alvo móvel enquanto Pedro/Nathan também trabalham nela.</why>
    </decision>
    <decision date="2026-06-09" author="João + Claude">
      <what>EPIC-02 introduziu TempUserGuard + @CurrentUser() (src/auth/) como placeholder de
      autenticação: lê o header `x-user-id` e popula `request.user = { id }`. Documentado em
      código como temporário, a ser substituído pelo guard JWT real do Pedro (PED-RF-02) sem
      alterar VehiclesController — o contrato `request.user.id` deve ser preservado.</what>
      <why>Auth do Pedro ainda não existe (Q02 em aberto) e EPIC-02 precisa de um `ownerId`
      para criar/filtrar veículos. Mantendo o mesmo shape de `request.user`, a troca pelo guard
      real é um diff mínimo.</why>
    </decision>
  </decisions>

  <!-- ═══════════════════════════════════════════════════════════
       DÍVIDAS TÉCNICAS IDENTIFICADAS
       ═══════════════════════════════════════════════════════════ -->
  <technical_debts>
    <debt priority="high" created="2026-06-09">
      Frontend tem telas completas com dados 100% hardcoded/mockados — falta camada de dados real
      (TanStack Query, Zustand, socket.io-client) e nenhuma dessas libs está instalada ainda.
    </debt>
    <debt priority="high" created="2026-06-09">
      MapPage usa um MapView SVG customizado (determinístico) em vez de MapLibre GL JS —
      JOA-RF-04 exige MapLibre com polyline/marcadores reais e downsampling.
    </debt>
    <debt priority="medium" created="PRE-SESSION">
      Tipos TypeScript compartilhados entre backend e frontend precisam de estratégia
      (package shared/ ou geração via Prisma) — frontend já definiu seus próprios tipos em
      features/shared/types/, que devem servir de referência para o schema Prisma do backend.
    </debt>
    <debt priority="low" created="2026-06-09">
      frontend/smart-drive/features/shared/types/telemetry.ts declara lat/lng/speedKmh/satellites/hdop
      como obrigatórios em TelemetryPoint, mas o schema Prisma (fonte: telemetry_contract v1.0) os
      trata como opcionais (GPS pode ser null). Ajustar o tipo do frontend para `?`.
    </debt>
    <debt priority="low" created="2026-06-09">
      Prisma 7 exige `moduleFormat = "cjs"` no generator (senão o client gerado é ESM e quebra
      ts-jest com "Cannot use import.meta") + moduleNameMapper no jest config para resolver
      imports `.js` do client gerado para `.ts` + NODE_OPTIONS=--experimental-vm-modules nos
      scripts de teste (o query compiler wasm do Prisma 7 usa dynamic import). Já configurado em
      backend/smart-drive/package.json — documentar para Pedro/Nathan se tiverem testes próprios
      que importam o Prisma Client gerado.
    </debt>
    <debt priority="low" created="2026-06-15">
      mocks/__tests__/factories.test.ts (frontend, commit 282840c do sprint-1) tem 6 erros de
      `tsc --noEmit`: usa strings cruas ('ELECTRIC', 'ONLINE', 'HARD_BRAKE', etc.) onde o tipo
      espera os enums FuelType/DeviceStatus/DrivingEventType. Não quebra os testes (vitest roda via
      transform sem typecheck), mas suja o tsc. Fix trivial: trocar pelos membros do enum.
    </debt>
    <debt priority="medium" created="2026-06-09">
      VehiclesController usa TempUserGuard (header `x-user-id`) em vez de auth JWT real —
      substituir pelo guard do Pedro (PED-RF-02) assim que disponível, mantendo
      `request.user = { id: string }` (src/auth/request-with-user.ts). Mesmo guard deve ser
      reaproveitado por Devices/Trips/Telemetry quando esses módulos exigirem dono autenticado.
    </debt>
    <debt priority="medium" created="2026-06-15">
      Flakiness de isolamento no backend: vehicles.service.integration.spec.ts falha
      intermitentemente quando o jest roda as suites EM PARALELO contra o mesmo Postgres
      (o teste "findAll não retorna veículos de outros usuários" sofre corrida de dados entre
      workers). Passa 100% serial (`jest --runInBand`) e isolado. Corrigir dando isolamento por
      suite (schema/transação por worker, ou banco dedicado de teste, ou maxWorkers=1 nos
      integration). Não afeta os testes de telemetry (não usam DB).
    </debt>
    <debt priority="low" created="2026-06-15">
      TelemetryController (POST /telemetry e /telemetry/dev/device-status) é mock/dev TEMPORÁRIO
      (EPIC-04) só para disparar o gateway sem hardware. Remover quando o POST /telemetry real do
      Pedro (PED-RF-06) existir; este deve chamar TelemetryGateway.emitTelemetryNew após persistir.
    </debt>
  </technical_debts>

  <!-- ═══════════════════════════════════════════════════════════
       BLOQUEIOS ATIVOS
       ═══════════════════════════════════════════════════════════ -->
  <blockers>
    <blocker id="B01" status="resolved" severity="critical" resolved_date="2026-06-09">
      Schema Prisma (JOA-TEC-01) implementado e migrado. Pedro e Nathan podem implementar seus
      módulos sobre backend/smart-drive/prisma/schema.prisma e o PrismaService global
      (importável via PrismaModule, @Global). Ler as "NOTAS PARA O TIME" no topo do
      schema.prisma antes de começar.
    </blocker>
  </blockers>

  <!-- ═══════════════════════════════════════════════════════════
       HISTÓRICO DE ATIVIDADES
       ═══════════════════════════════════════════════════════════ -->
  <history>
    <entry date="PRE-SESSION" type="planning">
      PRD analisado. Requisitos do João identificados e documentados em CLAUDE.json.
      Memórias do projeto gravadas. Documentação de requisitos do frontend gerada em markdown.
      Arquivos de configuração do Claude Code criados (CLAUDE.json, context.xml, credentials.xml, session-current.xml, session-next.xml).
    </entry>
    <entry date="sprint-1 (data exata não registrada)" type="implementation">
      5 commits de frontend: configuração de Vitest+RTL+MSW (test infra), MSW server/handlers/factories
      para todos os domínios (vehicles, devices, trips, telemetry, demo, dashboard), tipos TypeScript
      de domínio com enums (Vehicle, Device, Trip, TelemetryPoint, DrivingEvent, FuelEstimate, DrivingScore),
      sistema de tema dark/light com CSS custom properties (sdVars), e migração para arquitetura
      feature-based (features/dashboard, demo, devices, home, map, shared, shell, trips, vehicles).
      Branches feat/s1-* e release/sprint-1 criados. Esse trabalho NÃO estava refletido em session-current.xml.
    </entry>
    <entry date="2026-06-09" type="planning">
      Sessão de varredura: leitura de toda a documentação em docs/ e do código atual de
      firmware/, backend/ e frontend/. Constatada defasagem grande entre session-current.xml
      e o estado real (frontend muito mais avançado, backend/firmware em esqueleto).
      session-current.xml reescrito. Memórias de longo prazo gravadas (team roles, convenção
      de docs de sessão, referências de requisitos).
    </entry>
    <entry date="2026-06-09" type="planning">
      Plano de implementação em épicos elaborado e aprovado: EPIC-00 (schema Prisma, crítico)
      até EPIC-13 (QA final/ExpoIOT), cobrindo todos os JOA-RF/RNF/TEC. Plano salvo em
      /home/joao/.claude/plans/wobbly-coalescing-comet.md e replicado em session-next.xml
      (plan-002), com bottlenecks, dependências entre times e perguntas em aberto atualizadas.
    </entry>
    <entry date="2026-06-09" type="implementation">
      EPIC-00 implementado de ponta a ponta: `npx prisma init` no backend (com prisma.config.ts,
      .env, @prisma/client/prisma 7.8.0); prisma/schema.prisma modelado com 8 models + 6 enums
      a partir de features/shared/types/*, com notas de design para Pedro/Nathan; ajustes para
      Prisma 7 (remoção de `url` do datasource, `moduleFormat = "cjs"`, @prisma/adapter-pg);
      `npx prisma format/validate/generate` ok. Criados PrismaService/PrismaModule (global),
      ConfigModule + ValidationPipe globais em main.ts, e módulos placeholder VehiclesModule/
      DevicesModule/TripsModule/TelemetryModule/DemoModule/AnalysisModule, todos importados em
      AppModule. `nest build` ok. Criado backend/docker/docker-compose.yml (postgres:16-alpine)
      + backend/docker/.env.example, backend/smart-drive/.env.example, e .env de dev local
      preenchido (DATABASE_URL/JWT_SECRET/PORT/CORS_ORIGIN/NODE_ENV). Usuário instalou Docker e
      adicionou-se ao grupo docker (sudo usermod -aG docker joao). `docker compose up -d` subiu
      o Postgres; `npx prisma migrate dev --name init` aplicou a migration inicial com sucesso.
      Escritos 5 testes de integração Prisma (criação válida, FK inválida rejeitada, deviceCode
      único, TelemetryPoint sem GPS, cascade delete de Trip) — todos passando contra o Postgres
      real (`npm test`: 2 suites, 6 testes, 100% pass). Blocker B01 resolvido.
    </entry>
    <entry date="2026-06-09" type="implementation">
      Adicionado .gitignore raiz (exclui docs/credentials.xml, frontend/worktrees/,
      firmware/build/, .vscode/). Removido .git aninhado de backend/smart-drive (criado pelo
      `nest new`). Trabalho de EPIC-00 commitado e enviado ao GitHub em 2 commits: d17b2ad
      (docs/specs, firmware skeleton, configs raiz) e 9b643ae (backend EPIC-00 — Prisma,
      NestJS, Docker). Em seguida, EPIC-01 finalizado: frontend/smart-drive/.env.example
      criado (com exceção `!.env.example` no .gitignore do frontend), README.md raiz
      reescrito com setup passo a passo (banco/backend/frontend). Setup validado de ponta
      a ponta (`prisma migrate dev` idempotente, `npm run start:dev` ok).
    </entry>
    <entry date="2026-06-09" type="implementation">
      EPIC-02 implementado em branch feature/joa-rf-01-02-vehicles-module (primeira branch
      separada por épico, conforme nova convenção). Criados: src/auth/ (TempUserGuard,
      @CurrentUser(), RequestWithUser — placeholder de auth documentado, contrato
      request.user.id preservável para o guard real do Pedro); src/vehicles/dto/
      (sanitize-text.ts + spec, CreateVehicleDto + spec cobrindo todos os SPECs/edge cases de
      JOA-RF-01, UpdateVehicleDto via @nestjs/mapped-types PartialType — pacote instalado);
      VehiclesService (create/findAll/findOne/update/remove com soft delete e bloqueio
      cross-user via NotFoundException); VehiclesController (POST/GET/GET:id/PATCH:id/
      DELETE:id, @UseGuards(TempUserGuard)); VehiclesModule atualizado (deixou de ser
      placeholder). 35 testes (24 de DTO/sanitização + 11 de integração contra Postgres real)
      passando, suite completa 41/41, `nest build` e lint OK. Smoke test manual via curl
      (criação com sanitização de `<Fiat>`→`Fiat`, 401 sem x-user-id, 400 em consumo=0,
      PATCH parcial preservando campos, 404 em acesso cross-user, soft delete via DELETE)
      validado de ponta a ponta contra o Postgres local. Branch enviada ao GitHub e PR #1
      mergeado em main (https://github.com/Jg-365/smart-drive/pull/1).
    </entry>
    <entry date="2026-06-09" type="planning">
      EPIC-03 (kickoff): branch feature/joa-rnf-01-realtime-data-layer criada a partir de main.
      Levantamento de contexto feito (sem código escrito ainda):
      - websocket_events (docs/CLAUDE.json): server→client = telemetry:new, trip:eventDetected,
        trip:scoreUpdated, trip:fuelEstimateUpdated, device:statusChanged, trip:finished;
        client→server = subscribe:trip, unsubscribe:trip.
      - telemetry_contract v1.0 revisado (payload deviceId/vehicleId/sessionId/timestamp/gps/imu/
        events/battery, gps pode ser null).
      - Frontend: nenhuma lib de dados instalada ainda (confirma G02). package.json tem apenas
        next/react/vitest+RTL+MSW+tailwind. features/shared/types/* já tem TelemetryPoint,
        DrivingEvent (+enums), DrivingScore, FuelEstimate, Device (+DeviceStatus) — usar como
        tipos dos eventos WS. vitest.config.ts já configurado (jsdom, alias @/, setupTests.ts
        com import opcional de mocks/server para MSW) — mesma infra serve para testar o hook
        com mock de socket.io-client.
      Sessão pausada aqui por limite de uso, conforme pedido do João ("salve tudo, continuamos
      amanhã"). Próximos passos detalhados em session-next.xml (EPIC-03).
    </entry>
    <entry date="2026-06-15" type="implementation">
      EPIC-03 implementado de ponta a ponta (JOA-RNF-01). Sidequest antes: criado
      tools/telemetry-recorder/ (gravador de telemetria real via sensores do celular, CSV no
      formato do contrato v1.0) a pedido do Nathan para calibrar os detectores com dados do mundo
      real; diagnosticado e corrigido o motivo de o GPS não pedir permissão (contexto inseguro —
      precisa de https; servido via cloudflared tunnel) e adicionado aviso de isSecureContext no
      próprio HTML. Depois, EPIC-03: instaladas @tanstack/react-query, zustand, socket.io-client,
      maplibre-gl; criado lib/api/ (client.ts fetch tipado + ApiError, config.ts, telemetry.ts com
      fetchLiveTelemetry/fetchTripTelemetry, index.ts) com 4 testes; criado
      features/shared/realtime/ (store.ts Zustand + selectors atômicos, events.ts, useTelemetrySocket
      com reconexão 5x e fallback de polling 2s, TelemetryProvider, index.ts) com 9 testes (hook 6,
      store 3, provider 2) usando socket.io-client e lib/api mockados. Decisão do João: reconexão =
      5 tentativas + polling 2s. Corrigido bug latente em setupTests.ts (require não resolvia alias
      @/ → MSW global desligado em toda a suite; trocado por import estático). Suite frontend
      passou de 95 para 106 testes, todos verdes; tsc/eslint limpos nos arquivos novos (restam 6
      erros tsc PRÉ-EXISTENTES em mocks/__tests__/factories.test.ts, commit 282840c, strings cruas
      em vez de enums — dívida menor a corrigir à parte). PR #2 aberto e mergeado em main.
    </entry>
    <entry date="2026-06-15" type="implementation">
      EPIC-04 implementado na branch feature/joa-rf-03-websocket-gateway (a partir de origin/main,
      que já tem o PR #1/EPIC-02 mergeado). Instaladas @nestjs/websockets, @nestjs/platform-socket.io,
      socket.io (+ socket.io-client dev). src/telemetry/: telemetry-payload.dto.ts (contrato v1.0
      com validação aninhada via class-validator/@Type — GpsDto/ImuDto/EventsDto/BatteryDto),
      device-status.dto.ts, telemetry.events.ts (nomes WS espelhando o front), telemetry.mapper.ts
      (toLivePoint achata o payload no LiveTelemetryPoint, tripId=sessionId), telemetry.gateway.ts
      (TelemetryGateway socket.io com rooms trip:<id>, subscribe/unsubscribe, emitTelemetryNew/
      emitDeviceStatusChanged, CORS de CORS_ORIGIN), telemetry.controller.ts (mock/dev temporário
      POST /telemetry + /telemetry/dev/device-status), telemetry.module.ts (não-placeholder,
      exporta o gateway). 16 testes novos (DTO 9, gateway unit 5, integração com socket.io-client
      real 2 — prova o done_when: cliente que deu subscribe:trip recebe telemetry:new após POST
      /telemetry, e 400 sem deviceId). Corrigido bug latente de teste: specs de DTO quebravam com
      "Reflect.getMetadata is not a function" quando rodados sem um spec do Nest carregando
      reflect-metadata antes (os specs de vehicles passavam só por ordem de execução); adicionado
      "setupFiles": ["reflect-metadata"] ao jest config. Suite backend 57/57 SERIAL (em paralelo,
      vehicles integration flakeia por isolamento de Postgres — dívida registrada). nest build e
      eslint limpos. Próximo: EPIC-05 (Dashboard real, depende de EPIC-03 PR #2 + EPIC-04).
    </entry>
    <entry date="2026-06-15" type="implementation">
      Coordenação: mergeados PRs #3 (EPIC-04), #2 (EPIC-03) e #1 (EPIC-02) em main; conflitos dos
      docs de sessão reconciliados manualmente (current_focus, history, technical_debts, lista de
      épicos e completed_from_plan). main em 3036865 com EPIC-02/03/04. Criada branch
      feature/joa-rf-03-dashboard-realtime a partir de main para o EPIC-05.
      EPIC-05 (JOA-RF-03 Dashboard em tempo real): store ganhou lastPacketAt (setado em
      ingestPoint) + seletor useLastPacketAt; novo useLiveStatus (deriva online/offline por
      staleness 5s + connection + device:statusChanged; tica 1s) e isValidSpeed/MAX_VALID_SPEED_KMH
      (edge >300). features/dashboard/derive.tsx com helpers comuns (G, SCORE_HINT, EVENT_META,
      isHighSeverity). DashboardPage (desktop) e MobileLivePage (mobile) reescritos: decompostos em
      subcomponentes que assinam só sua fatia do store (RNF-01), cobrindo os 12 SPECs + edge cases
      (GPS null placeholder, velocidade inválida, burst de eventos, reconexão, estado vazio+CTA).
      TelemetryProvider plugado em app/page.tsx com tripId de dev via NEXT_PUBLIC_DEV_TRIP_ID (null
      em prod). Dev feeder em tools/telemetry-feeder/ (feed.mjs + README): POST /telemetry em loop
      simulando direção, exercita gateway→WS→store→dashboard. Favicon: copiado assets/Vector.svg →
      app/icon.svg (convenção de file do Next, confirmada no build como rota /icon.svg). 26 testes
      novos (useLiveStatus 7, DashboardPage 14, MobileLivePage 5); suite frontend 132/132, tsc sem
      novos erros, build OK, eslint limpo (resta 1 warning pré-existente: onNav em MobileScreenWrapper).
      LIMITAÇÃO: score/consumo/eventos só preenchem quando o caminho do Nathan emitir
      trip:scoreUpdated/fuelEstimateUpdated/eventDetected — o feeder dispara só telemetry:new.
      Próximo: EPIC-06 (Mapa MapLibre, JOA-RF-04).
    </entry>
    <entry date="2026-06-15" type="implementation">
      PR #4 (EPIC-05) mergeado em main (8799393). Criada branch feature/joa-rf-04-maplibre-map.
      EPIC-06 (JOA-RF-04 Mapa em tempo real): features/shared/geo.ts (puro) com isValidCoord,
      toLngLat, downsample (MAX_POLYLINE_POINTS=500). Store ganhou route: LngLat[] (ingestPoint só
      acumula coord válida → marcador congela e polyline não quebra com GPS null) + seletor useRoute.
      LiveMap.tsx (MapLibre imperativo: style raster escuro CARTO inline, NavigationControl/zoom,
      polyline com downsampling, marcadores de evento por severidade, marcador do veículo,
      centralização no 1º ponto, fallback no evento 'error' de tile). LiveMapContainer liga o store.
      MapPage usa next/dynamic ssr:false (resolve G03 — MapLibre usa WebGL/window). CSS do maplibre
      em app/layout.tsx. 15 testes novos (geo 6, store route 1, LiveMap 4 com maplibre-gl mockado +
      utils); suite frontend 143/143, tsc/build limpos (1 warning de _opts em mock de teste).
      Sem deps novas (maplibre-gl já instalado no EPIC-03). NOTA: dashboard/mini-mapas seguem em
      MapView SVG (migração opcional EPIC-10); integração com GET /trips/:id/route (replay, Pedro)
      pendente. Fim da sessão do dia.
    </entry>
  </history>

</session>
